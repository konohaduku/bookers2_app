<% if @book.image.attached? %>
    <%= image_tag list.image, size: "100x100" %>
  <% else %>
    <%= image_tag 'no_image', size: "100x100" %>
  <% end %>
  
  <%=link_to "Edit", edit_user_registration_path %>
  
<h1>New book</h1>
<%= form_with model: Book.new do |f| %>
  <h4>title</h4>
  <%= f.text_field :Title %>
	
  <h4>opinion</h4>
  <%= f.text_area :Body %>
	
  <%= f.submit 'Creat Book' %>
<% end %>